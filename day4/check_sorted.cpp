#include<bits/stdc++.h>
using namespace std;


int main(){
    int n;
    cin>>n;
    int arr[n], arr2[n];
    for(int i = 0; i < n; i++){
        int x;
        cin >> x;
        arr[i] = x;
        arr2[i] = x;
        }
        sort(arr, arr + n);
        int count = 0;
        for(int i = 0; i < n; i++){
            if(arr[i] != arr2[i])
        }
        count++;
}
